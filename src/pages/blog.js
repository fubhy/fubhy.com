import React from 'react';
import Page from '../components/Page';
import styles from './index.module.css';

const BlogPage = ({ transition }) => (
  <Page title="Blog" transition={transition}>
    Nothing to see here.
  </Page>
);

export default BlogPage;
